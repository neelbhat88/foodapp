<h1>New Fridge</h1>

<div class="row">
  <div class="span6 offset3">
  	<%= render 'shared/error_messages', :model => @fridge %>
    <%= form_for(@fridge) do |f| %>

      <%= f.label :name %>
      <%= f.text_field :name %>          

      <%= f.submit "Save", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>